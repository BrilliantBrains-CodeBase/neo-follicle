import RegenerativeTreatments from '../components/treatments/RegenerativeTreatments'
import ScalpHealth from '../components/treatments/ScalpHealth'
import SurgicalTreatments from '../components/treatments/SurgicalTreatments'
import TreatmentCategories from '../components/treatments/TreatmentCategories'
import TreatmentJourney from '../components/treatments/TreatmentJourney'
import TreatmentMatcher from '../components/treatments/TreatmentMatcher'
import TreatmentsCta from '../components/treatments/TreatmentsCta'
import TreatmentsHero from '../components/treatments/TreatmentsHero'
import WhyDiagnosisFirst from '../components/treatments/WhyDiagnosisFirst'

/**
 * Treatments Overview -- the site's treatments index.
 *
 * Built from content/treatment-overview/Neo Follicle Website Content-2.md
 * ("VOLUME 3 -- TREATMENTS OVERVIEW"), sections 1 through 9 in the doc's own
 * order. Each section is one component under src/components/treatments/, and
 * each of those owns the docblock for its design source and its departures.
 *
 * THERE IS NO GENERATED MARKER AT THE TOP OF THIS FILE, DELIBERATELY.
 * scripts/gen-pages.mjs overwrites any page file that still carries
 * `// GENERATED by scripts/gen-pages.mjs. Do not edit by hand.` and leaves the
 * rest alone (see its `skipped` guard). Re-adding that line would mean the next
 * `npm run gen:pages` silently destroys this page. home.tsx is the precedent.
 *
 * WHY THE DOC'S SUGGESTED URL IS NOT USED. The doc proposes
 * /hair-loss-treatments-bangalore. This page is served at the EXISTING
 * /hair-loss-treatment-in-bangalore/, which is the live treatments overview:
 * it is indexed, it is in the sitemap, src/config/nav.ts links it from the
 * footer as "Hair Loss Treatments", and scripts/verify-seo.mjs asserts its
 * title, description, canonical and JSON-LD byte-for-byte against the
 * neofollicle-seo-backup capture. Building here costs no redirect and no
 * ranking; a new slug would have cost both.
 *
 * PagePlaceholder is deliberately NOT used. It renders seoFor(slug).h1, which
 * would emit a second <h1> under TreatmentsHero's -- the content doc's own
 * rule, and home.tsx's, is exactly one per page.
 *
 * The <h1> TreatmentsHero renders is the CAPTURED SEO h1 from src/seo/pages.ts,
 * not the doc's heading. The reasoning, and the one-line change to flip it, are
 * at the top of TreatmentsHero.tsx.
 *
 * Section order is the doc's, with one structural decision recorded in
 * TreatmentCategories.tsx: SECTION 2 is the in-page index and SECTIONS 3, 4
 * and 5 carry the outbound links, so no treatment page is linked twice from
 * this URL.
 */
export default function HairLossTreatmentInBangalore() {
  return (
    <>
      <TreatmentsHero />
      <TreatmentCategories />
      <RegenerativeTreatments />
      <SurgicalTreatments />
      <ScalpHealth />
      <TreatmentMatcher />
      <WhyDiagnosisFirst />
      <TreatmentJourney />
      <TreatmentsCta />
    </>
  )
}
