/**
 * /video-gallery/
 *
 * No `// GENERATED` marker, and no PagePlaceholder -- see the note at the top
 * of image-gallery.tsx, which applies here identically.
 *
 * The h1 is the captured string from src/seo/pages.ts verbatim, including the
 * literal ampersand in "Hair Transplant & Hair Loss Treatments". It lives in
 * content/gallery/video-gallery.ts.
 */
import VideoGalleryPage from '../components/gallery/VideoGalleryPage'

export default function VideoGallery() {
  return <VideoGalleryPage />
}
